# EVM Project Tool 0.5.0

## Resumen

En el bloque de indicadores consolidados, CPI y SPI se muestran en tres renglones (sigla, ícono, interpretación) con el mismo ancho máximo que el resto de tarjetas. La tabla de actividades conserva el badge en una sola línea.

Spec `evm-project-tool-frontend` Fase 8: 1/1 tareas. Vitest / lint / build: **PASA**. E2E Compose: SPA `:8080` y API `:8000` HTTP 200.

## Cambios

### frontend (`evm-project-tool-frontend`) — PR #19 (develop)

- `CpiSpiBadge` con `layout="inline"|"stacked"`.
- Consolidados: `layout="stacked"`; grid con `min-width: 0` para no ensanchar las tarjetas.
- Tabla: sigue `inline` + `nowrap` (v0.4.0).
- Spec: CA-08.5 y `design.md` §7.4.

## Validación pre-release

| Ámbito | Resultado |
|:---|:---|
| Vitest / ESLint / `npm run build` | PASS |
| Docker Compose E2E (SPA + API HTTP) | PASS |
